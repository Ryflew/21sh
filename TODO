
----------- TODO --------------

- Unset / Export
- Ajouter unset / export a la commande help
- Norme
- Leaks
- Pour chacun refaire tous les headers a son nom (libft etc compris !)

---------- BONUS --------------

- Resize de la fenetre

------------ A TESTER ---------

- aggregation de fichier <&
- Testez l'existence et le fonctionnement du heredoc "<<", avec ses options et cas particuliers.

------------ BUG --------------

<<<<<<< HEAD
- ls [!2aiT]* ca affiche ls [quitaiT]* en input (backslash)
{
	t_list  *lexems;
	t_token *token;
	char	*line;

    lexems = sh->lexer->lexems;
    while (lexems)
    {
		token = (t_token*)lexems->data;
		while (ft_isblank(*line) || *line == '\' || *line == '"' || *line '\''|| line == '`')
			++line;
        if (token->type == HIST)
        {
		}
		else
		{
			line += ft_strlen(token->value);
			lexems = lexems->next;
		}
    }
}
Manque les backslashs

--- Bonus Slider A Dire En soutenance pour le 21 ---

- && et ||
- Commande help
- Tous les builtins sont a la norme POSIX
- Unset / Export
- Command !history