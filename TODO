
-------- BUG -------------
BASILE :
ls */*[.ch]
ls */*.c

VICTOR :

- cat | cat | cat | ls // fixe normalement a tester.
- A tester avant de rendre: "history -d 5;ls"

BASILE || VICTOR:
- echo "`ls -l`" != echo `ls -l`
- ls badfile 2>&1 1>&2 | tr o O
- lexer_string list of st_op

-------- LIST BONUS 21sh -------------
- cmd "history"
- ctrl + r
- cmd "help"
- hash table
- cmd "read"
- && 
- ||
- ()
- globing (*)
