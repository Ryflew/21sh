
-------- BUG -------------
BASILE :
- diff <(echo -n abc | openssl base64) <(echo -n abc | openssl base64) // je le note mais osef
- "`[`" // ca explose le shell

VICTOR :

BASILE || VICTOR:

-------- LIST BONUS 42sh -------------
- ctrl + r
- cmd "help"
- hash table
